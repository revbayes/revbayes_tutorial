alpha ~ dnUnif(0,50)
site_rates := fnDiscretizeGamma(alpha, alpha, 4)
moves[1] = mvScale(alpha)

mymodel= model(alpha)

monitors[1] = mnScreen(alpha)
monitors[2] = mnModel(filename="out.log")

mymcmc = mcmc(mymodel, moves, monitors)
mymcmc.run(1e3)
